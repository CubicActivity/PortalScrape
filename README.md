# PortalScrape 
Multithreaded Python web scraper built for gathering & storing hiring data.

#### Requirements to run the script:
  - Python version 3.13
  - Inside the directory './Fetchers' rename the file '.env.example' to '.env' and replace "your_token_here" with your actual findwork.dev token!
